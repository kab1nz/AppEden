<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a.appeden">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/ELeden"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InicioActivity" />
        <activity android:name=".Habitacion" />
        <activity android:name=".restaurante" />
        <activity android:name=".eventos" />
        <activity android:name=".oferta" />
        <activity android:name=".reservaoferta" />
        <activity android:name=".instalacionesservicios" />
        <activity android:name=".lugar"></activity>
    </application>

</manifest>